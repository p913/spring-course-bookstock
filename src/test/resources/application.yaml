spring:
  shell:
    interactive:
      enabled: false

  data:
    mongodb:
      database: bookstocktest
      port: 0

logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG
